apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fio-data-pvc
  namespace: my-ripsaw
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: ocs-storagecluster-ceph-rbd
  resources:
    requests:
      storage: 1G
  volumeMode: Filesystem 
